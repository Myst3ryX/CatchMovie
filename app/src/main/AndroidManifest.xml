<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.myst3ry.catchmovie">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_title"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Toolbar">
        <activity android:name=".ui.activity.BaseActivity" />
        <activity android:name=".ui.activity.NavDrawerBaseActivity" />
        <activity android:name=".ui.activity.MoviesActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.TvShowsActivity" />
        <activity android:name=".ui.activity.ActorsActivity" />
        <activity android:name=".ui.activity.SettingsStubActivity" />
        <activity android:name=".ui.activity.MovieDetailActivity" />
        <activity android:name=".ui.activity.TvShowDetailActivity" />
        <activity android:name=".ui.activity.ActorDetailActivity" />
    </application>

</manifest>