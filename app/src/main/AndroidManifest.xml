<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.myst3ry.catchmovie">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_title"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.activity.MoviesActivity"
            android:theme="@style/AppTheme.Toolbar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.TvShowsActivity"
            android:theme="@style/AppTheme.Toolbar" />
        <activity
            android:name=".ui.activity.ActorsActivity"
            android:theme="@style/AppTheme.Toolbar" />
        <activity
            android:name=".ui.activity.SettingsStubActivity"
            android:theme="@style/AppTheme.Toolbar" />
        <activity android:name=".ui.activity.NavDrawerBaseActivity" />
    </application>

</manifest>