<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.myst3ry.catchmovie">

    <application
        android:name=".CatchMovieApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_title"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Toolbar">
        <activity android:name=".ui.activity.NavDrawerBaseActivity" />
        <activity android:name=".ui.activity.movie.MoviesActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.tvshow.TvShowsActivity" />
        <activity android:name=".ui.activity.actor.ActorsActivity" />
        <activity android:name=".ui.activity.SettingsStubActivity" />
        <activity android:name=".ui.activity.movie.MovieDetailActivity" />
        <activity android:name=".ui.activity.tvshow.TvShowDetailActivity" />
        <activity android:name=".ui.activity.actor.ActorDetailActivity" />
    </application>

</manifest>